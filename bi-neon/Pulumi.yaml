name: bi-neon
description: Neon provisioning for The Baker's Index
runtime:
  name: nodejs
  options:
    packagemanager: yarn
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
packages:
  neon:
    source: terraform-provider
    version: 1.0.2
    parameters:
      - kislerdm/neon
